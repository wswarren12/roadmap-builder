# Secrets & local env — never commit
.env
.env.*
*.local

# Dependencies & build output
node_modules/
.next/
coverage/
test-results/
playwright-report/
*.tsbuildinfo

# OS noise
.DS_Store
*.log

# Local agent/kit tooling — stays on disk, not in the public repo
.agents/
.claude/
.mcp.json
.playwright-mcp/
.vscode/
AGENTS.md
CLAUDE.md
pln-app.config.json
skills-lock.json
